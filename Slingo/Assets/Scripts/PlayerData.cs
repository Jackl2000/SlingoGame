using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerData : MonoBehaviour
{
    public spin spinScript;

    public float balance = 999;

    //public void Stakes()
    //{
    //    if (spinScript.spinLeft == 8)
    //    {
    //        spinScript.stakes += spinScript.spinBets;
    //    }
    //    if (spinScript.spinLeft < 0 && !spinScript.isSpinning)
    //    {
    //        spinScript.stakes += spinScript.PriceCaculator();
    //    }
    //    spinScript.spentText.text = "Stakes: " + spinScript.stakes.ToString("F2") + " kr";

    //}

}
